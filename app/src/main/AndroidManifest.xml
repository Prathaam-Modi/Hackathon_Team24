<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scvote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SCVOTE">
        <activity android:name=".LiveCounting"></activity>
        <activity android:name=".HomeActivity3" />
        <activity android:name=".HomeActivity2" />
        <activity android:name=".CanListActivity" />
        <activity android:name=".AddCandidateActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".AdminPageActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>